/**
 * Integration tests for StrategyRegistry — DEPRECATED.
 * StrategyRegistry has been removed from the protocol (PR #78).
 * This file is kept as a placeholder. Tests are skipped.
 */

import { describe, it } from "vitest";

describe.skip("StrategyRegistry (removed)", () => {
  it("strategyCount — skipped (contract removed)", () => {});
  it("listStrategies — skipped (contract removed)", () => {});
});
